@model IEnumerable<WebApplication2.Models.Disasters>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Disasters:</h1>

<p>
    <a asp-action="Create" class="btn btn-orange">Create New</a>
    <style>
        .navbar-brand,
        .nav-link.custom-text-color {
            color: #ff6f00 !important;
            position: relative;
        }

            .navbar-brand::after {
                content: "";
                display: block;
                width: 100%;
                height: 2px;
                background-color: #ff6f00;
                position: absolute;
                bottom: -10px;
                left: 0;
            }

        .btn-orange {
            background-color: #ff6f00;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
        }

            .btn-orange:hover,
            .btn-orange:active {
                background-color: #ff8500;
                color: white;
                text-decoration: none;
            }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            border: 2px solid #ff6f00;
            padding: 8px;
            text-align: left;
            color: #ff6f00; 
        }

        th {
            background-color: #ff6f00;
            color: white;
        }
    </style>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AidType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AidType)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.DisID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.DisID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.DisID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
